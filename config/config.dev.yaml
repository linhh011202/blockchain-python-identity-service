app:
  name: "identity-service"
  env: "dev"

database:
  url: "postgresql://neondb_owner:npg_CaDH1pO5tmBP@ep-wild-night-a1m31hrz-pooler.ap-southeast-1.aws.neon.tech/blockchain-python?sslmode=require&channel_binding=require"

security:
  jwt:
    algorithm: "HS256"
    access_token_ttl_minutes: 15
    refresh_token_ttl_days: 14
    secret: "CHANGE_ME_DEV_SECRET"

rbac:
  default_role: "user"